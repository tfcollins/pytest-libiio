# {{ device.name }}

{% if device['device_attributes']|length > 1 %}
## Device Attributes

| Name | Default value |
| ---- | ------------- |
{% for attr in device['device_attributes'] -%}
| {{ attr }} | {{ device['device_attributes'][attr]['value'] }} |
{% endfor %}

{% endif %}

## Channel Attributes

{% for chan in device['channel_attributes'] -%}

### {{ chan }}

| Name | sysfs filename | Default value |
| ---- | -------------- | ------------- |
{% for attr in device['channel_attributes'][chan] -%}
| {{ attr }} | {{ device['channel_attributes'][chan][attr]['filename'] }} | {{ device['channel_attributes'][chan][attr]['value'] }} |
{% endfor %}

**iio_attr example:**

```bash
iio_attr -u ip:analog -c {{ device['name'] }} {{ chan }} {{ device['channel_attributes'][chan].keys() | list | first }}
```

**Python bindings example:**

```python
import iio
ctx = iio.Context("ip:analog")
dev = ctx.find_device("{{ device['name'] }}")
chan = dev.find_channel("{{ chan }}")
rval = chan.attrs["{{ device['channel_attributes'][chan].keys() | list | first }}"].value
```

***
{% endfor %}
